export interface Theme {
    baselineColor: string
    primaryColor: string
    secondaryColor: string
    shadowColor: string
    backgroundColor: string
    button:{
        color:string
    }
    toggle:{
        color:string
    }
    linearProgressBar:{
        color:string
    }
    roundProgressBar:{
        color:string
    }
}